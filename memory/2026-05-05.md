# 2026-05-05 日志

## 记事本++ & CudaText 右键菜单修复

- 用户反馈 Notepad++ 和 CudaText 右键菜单打开文件报错
- 根因：注册表中存储的是 `%%1`（双%），Windows 传给程序的参数占位符应为 `%1`（单%）
- `reg add` 在直接命令行中 `%1` 无需转义，直接写；只有 .bat/.cmd 脚本中才需要 `%%1` 转义
- 已修复两个注册表项的 `"%1"` 值，右键菜单恢复正常